@model MiProyecto1.Models.Transporte

@{
    ViewBag.Title = "Actionventa";
}

<h2>Actionventa</h2>

@using (Html.BeginForm()) {

    <div class="form-horizontal">
        <div class="form-group">
            @Html.Label("Numero", htmlAttributes: new { @class = "control-label col-md-2" })
            @Html.TextBoxFor(model => Model._numero, htmlAttributes: new { @class = "form-control col-md-10" })
        </div>

        <div class="form-group">
            @Html.Label("Numero", htmlAttributes: new { @class = "control-label col-md-2" })
            @Html.TextBoxFor(model => Model._numero, htmlAttributes: new { @class = "form-control col-md-10" })
        </div>
    </div>
}